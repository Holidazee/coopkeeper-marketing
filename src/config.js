// Central place to change where the "Log in" and primary CTA buttons point.
// Update this single constant when the dashboard moves.
export const DASHBOARD_URL = "https://app.coopkeeper.net/";

// Signup deep-link — change later if you have a distinct signup route.
export const SIGNUP_URL = DASHBOARD_URL;

// Marketing site base URL — used in metadata / canonical if you need it in JS.
export const SITE_URL = "https://coopkeeper.net/";
